/**
 * A dependency graph analyzes which files depend on what. This is useful in how files are compiled. Ollie lang
 * does not allow for partial builds, only complete builds are used in the langauge
*/

#ifndef DEPENDENCY_GRAPH_H
#define DEPENDENCY_GRAPH_H


#endif
