/**
 * Author: Jack Robbins
 * This multifile program is designed to test dependencies that are several orders deep
 */

$module zero_checker;


pub fn is_zero(x:i32) -> i32 {
	ret x == 0 ? true else false;
}
