
fn tester(i:u32) -> i32{
	ret 1;
}

/**
 * For switch statement testing
 */
pub fn main() -> i32{
	let a:mut i32 = 23;
	
	if(a == 32) {
		let k:i32 = 232332222;
	} else {
		let p:i32 = -2322;
	}
	
	ret 0;
}
