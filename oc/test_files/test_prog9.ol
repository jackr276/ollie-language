
fn tester(i:u32) -> i32{
	ret 1;
}

/**
 * For switch statement testing
 */
fn main() -> i32{
	
	let mut a:i32 := 23;
	
	if(a == 32) then{
		let k:i32 := 232332222;
		
	} else {
			let p:i32 := -2322;
		}
	
	ret 0;
}
