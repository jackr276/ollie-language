fn main(u32 argc, char** argv)->i32{
	let i32 i := 0;
	let i32 a := 0;
	let i32 v := 0;
	let i32 b := 0;
	let i32 j := 0;
	let i32 sadf := 0;
	
	/*
		if(i <= 0) then {
			asn a:= a + 1;
		} else if( i == 1) then{
			asn a := 23232;
			if(a == 3232) then{
				asn a := a + 1;
			} else {
				asn a := a + 2;
			}
		} else {
			asn a := 0x23a;
		}

		//Just some junk
		let i32 sadfa := 232;

		asn a := a + 323;

	} while(a <= 23);
	*/

	while(a <= 32) do {
		asn a := a + 323;
	}

	ret j + a;
}

