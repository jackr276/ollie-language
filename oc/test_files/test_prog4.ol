fn main(u32 argc, char** argv)->i32{
	let u32 i := 0;
	let u32 a := 0;
	let u32 v := 0;
	let u32 b := 0;
	let u32 j := 0;
	let u32 sadf := 0;

	if(i == 0) then {
		if(j == a) then{
			asn a := 3;	
		} else if (j > a) then {
			ret j;
		}
	} else if( i == 1) then{
		let u32 i_copy := i;
	} else {
		let u32 j_copy := i;
	}

	ret j + a;
}

