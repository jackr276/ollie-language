fn main(u32 argc, char** argv)->i32{
	let i32 i := 0;
	let i32 a := 0;
	let i32 v := 0;
	let i32 b := 0;
	let i32 j := 0;
	let i32 sadf := 0;
	
	/*
	while(a <= 32) do {
		if(i <= 0) then {
			asn a:= a + 1;
		} else if( i == 1) then{
			asn a := 23232;
			if(a == 3232) then{
				asn a := a + 1;
			} else {
				asn a := a + 2;
			}
		} else {
			asn a := 0x23a;
		}

		//Just some junk
		let i32 sadfa := 232;

		asn a := a + 323;
	}
	*/

	for(let u32 i := 3; i < 232; asn i := i + 1) do{
		asn a := a + 1;
		continue when (a == 32);
		//break when(a == 32);
		let u32 masdfasdf := 232;
	}


	let f32 j_copy := 32.0;

	ret j + a;
}

