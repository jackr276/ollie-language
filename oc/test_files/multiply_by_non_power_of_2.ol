/**
* Author: Jack Robbins
* Test a case where we need to multiply by a non-power of 2
*/

pub fn signed_non_power_of_2(x:i32) -> i32 {
	ret x * -7;
}


pub fn unsigned_power_of_2(x:u32) -> u32 {
	ret x * 5u;
}


pub fn main() -> i32 {
	ret 0;
}
