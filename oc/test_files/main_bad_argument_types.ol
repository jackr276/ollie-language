/**
* Author: Jack Robbins
* This file tests handling of a main function with an invalid parameter type
*/


/**
* Let's change the types and see how we handle it
*/
pub fn main(argc:i64, argv:i32**) -> i32{
	ret **argv + argc + x;
}
