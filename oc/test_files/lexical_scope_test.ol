/**
* Author: Jack Robbins
*/

pub fn main() -> i32 {
	let x:mut i32 = 3;
	let y:mut i32 = 5;
	let z:mut i32 = 3;

	if( x == 0) {
		let x:mut i32 = 35;
		let y:mut i32 = 55;

		 z = x + y;
	} else {
		let x:mut i8 = 3;
		let y:mut i8 = 2;
		ret x / y;
	}

	ret x * y + z;
}
