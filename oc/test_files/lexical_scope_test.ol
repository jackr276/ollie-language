/**
* Author: Jack Robbins
*/

pub fn main() -> i32 {
	let mut x:i32 := 3;
	let mut y:i32 := 5;
	let mut z:i32 := 3;

	if( x == 0) {
		let mut x:i32 := 35;
		let mut y:i32 := 55;

		 z := x + y;
	} else {
		let mut x:i8 := 3;
		let mut y:i8 := 2;
		ret x / y;
	}

	ret x * y + z;
}
