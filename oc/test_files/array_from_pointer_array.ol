/**
* Author: Jack Robbins
* Test the result of getting an array from a 2D non-contiguous pointer array
*/

pub fn get_pointer_from_2d_array(x:i32**) -> i32* {
	ret x[2];
}

pub fn main() -> i32 {
	ret 0;
}
