fn main(u32 argc, char** argv) -> i32{
	while(argc < 5) do {
		declare i:u32;
		i := 3;
	}

	//Array of addresses
	declare address_array:u32*[10];
	
	let mine:my_enum := ENUM_TYPE_1;

	declare mut j:u32;

	let long:i64 := 234l;

	
	for(j := 0; j <= 33; j++) do {
		j := j - 3;
	}


	ret j;

	let my_int:u32 := 234;
}
