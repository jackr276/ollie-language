fn main(argc:u32, argv:char**) -> i32{
	while(argc < 5) do {
		declare i:u32;
		i := 3;
	}

	//Array of addresses
	declare address_array:u32*[10];


	let long:i64 := 234l;

	for(let mut j : u32 := 0; j <= 33; j := j + 1) do {
		j := -32ul;
	}


	ret 342U;
}
