declare structure my_struct {
	u_int32 i := 3,
	u_int32 j,
	u_int32 prog
};

func my_func(u_int32* error_code) -> u_int32{
	let u_int32 i := 0;
	let u_int32 j := 0;
	let u_int32 k := 0;

	main(i, j, k); 
}
