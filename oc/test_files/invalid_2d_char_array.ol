/**
* Author: Jack Robbins
* Test an invalid case where a user tries to initialize a global char array with mismatched lengths
*/

//This will not work. In order for this to work they must all be the same length
let global_char_arr:char[][] = ["Hello", "Alien", "ok"];

pub fn main() -> i32 {
	ret 0;
}
