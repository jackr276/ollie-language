/**
* Author: Jack Robbins
* This file tests handling of a main function with an invalid order
*/


/**
* We'll try placing argc and argv in the reverse order. This should fail
* on parse
*/
pub fn main(argv:char**, argc:i32) -> i32{
	ret **argv + argc + x;
}
