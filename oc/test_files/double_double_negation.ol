/**
* Author: Jack Robbins
* Test the resiliency of the local constant system to recognize and block duplicate
* values
*/

pub fn add_negated_doubles(x:f64, y:f64) -> f64 {
	ret -x + -y;
}


pub fn main() -> i32 {
	ret 0;
}
