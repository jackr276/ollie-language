name: Stack Data Area Test

on:
  push:
    branches: 
      - [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Stack Data Area Test
      run: make test_data_area-CI
