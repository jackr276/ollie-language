name: Parser test

on:
  push:
    branches: 
      - '*'
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Parser test
      run: make ptest-CI
